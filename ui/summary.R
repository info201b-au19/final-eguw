# Summary page
summary <- tabPanel(
  "Summary",
  titlePanel("Summary"),
  
  # Sidebar layout
  sidebarLayout(
    # Controls
    sidebarPanel(
      # Controls
    ),
    # Visualization
    mainPanel(
      # Visualization
    )
  )
)