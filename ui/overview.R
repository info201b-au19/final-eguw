# Define overview page layout
overview <- tabPanel(
  "Overview",
  titlePanel("Overview"),
  
  # Sidebar layout
  sidebarLayout(
    # Controls
    sidebarPanel(
      # Controls
    ),
    # Visualization
    mainPanel(
      # Visualization
    )
  )
)